[package]
name = "limnifs-bench"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "State-of-the-art benchmark suite for LimniFS"

[[bin]]
name = "limnifs-bench"
path = "src/main.rs"

[dependencies]
limnifs-core = { path = "../limnifs-core" }
limnifs-write = { path = "../limnifs-write" }
clap.workspace = true
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.rust]
unsafe_code = "allow"

[lints.clippy]
pedantic = "warn"
