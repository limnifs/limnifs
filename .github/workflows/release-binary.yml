# Build static CLI binaries for `limnifs` on Linux, macOS, Windows.
#
# Triggered on release tags (v*.*.*) and via workflow_dispatch.
# Uploads binaries to the corresponding GitHub Release.
#
# Uses a fully-static musl build on Linux for maximum portability.

name: Release Binaries

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl, aarch64-unknown-linux-musl

      - name: Build x86_64 (musl, static)
        run: cargo build --release --target x86_64-unknown-linux-musl -p limni

      - name: Build aarch64 (musl, static)
        run: cargo build --release --target aarch64-unknown-linux-musl -p limni

      - name: Package binaries
        run: |
          mkdir -p dist
          tar -czf dist/limni-linux-x86_64.tar.gz -C target/x86_64-unknown-linux-musl/release limni
          tar -czf dist/limni-linux-aarch64.tar.gz -C target/aarch64-unknown-linux-musl/release limni

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: limni-linux
          path: dist/

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-apple-darwin, aarch64-apple-darwin

      - name: Build x86_64
        run: cargo build --release --target x86_64-apple-darwin -p limni

      - name: Build aarch64 (Apple Silicon)
        run: cargo build --release --target aarch64-apple-darwin -p limni

      - name: Package binaries
        run: |
          mkdir -p dist
          tar -czf dist/limni-macos-x86_64.tar.gz -C target/x86_64-apple-darwin/release limni
          tar -czf dist/limni-macos-aarch64.tar.gz -C target/aarch64-apple-darwin/release limni

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: limni-macos
          path: dist/

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-gnu

      - name: Build x86_64 (GNU)
        run: cargo build --release --target x86_64-pc-windows-gnu -p limni

      - name: Package binaries
        shell: bash
        run: |
          mkdir -p dist
          7z a dist/limni-windows-x86_64.zip ./target/x86_64-pc-windows-gnu/release/limni.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: limni-windows
          path: dist/

  release:
    needs: [build-linux, build-macos, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/checkout@v7

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist/

      - name: Upload to release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            dist/limni-linux/*.tar.gz
            dist/limni-macos/*.tar.gz
            dist/limni-windows/*.zip
          generate_release_notes: true
          fail_on_unmatched_files: true
